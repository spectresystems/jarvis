<UserControl x:Class="Jarvis.Views.Settings.UpdateSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

    <StackPanel>
        <CheckBox x:Name="CheckForUpdates">Check for updates</CheckBox>
        <CheckBox x:Name="IncludePreviews" Margin="16,5,0,0" IsEnabled="{Binding ElementName=CheckForUpdates, Path=IsChecked}">Include previews</CheckBox>
    </StackPanel>

</UserControl>
